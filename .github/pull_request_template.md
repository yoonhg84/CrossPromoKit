Closes #

## 요약

<!-- 무엇을 왜 바꿨는지 두세 문장. 공개 API가 바뀌면 여기서 밝힙니다. -->

## 변경 사항

<!-- 파일 또는 동작 단위로. -->

## 근거

<!-- 선택한 방식과 그 이유. 검토했다가 탈락시킨 대안이 있으면 왜 탈락시켰는지도 함께. -->

## 검증

<!-- 실행한 명령과 결과를 그대로. 테스트를 추가했다면 프로덕션 코드를 일부러 깨뜨려 실패하는 것까지 확인했는지 적어 주세요. -->

- [ ] 공개 API나 문서를 바꿨다면 `README.md`와 `README.ko.md` **양쪽**에 반영했습니다
- [ ] `swiftlint lint --strict`와 시뮬레이터 `xcodebuild test`를 실행했습니다 (`Example/` 변경 시 데모 빌드까지)

## 사람이 확인해야 할 것

<!-- 자동 검증으로 덮을 수 없는 것(시뮬레이터 실행 화면, SKOverlay, VoiceOver 등). 없으면 "없음". -->
