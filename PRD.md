# CrossPromoKit PRD

이 문서에는 **코드·이슈·PR을 읽어서 알 수 없는 것만** 적습니다. 기능 목록, 데이터 모델,
프로젝트 구조, 검증 명령은 각각 코드·이슈 히스토리·`CLAUDE.md`·README가 정본입니다.
여기에 복사해두면 코드가 바뀌는 순간 거짓이 됩니다.

## 왜 만들었는가

여러 앱을 가진 개발자가 자기 포트폴리오 안에서 사용자를 순환시키려면, 앱마다 프로모션 화면을
새로 만들고 앱을 추가할 때마다 전부 다시 배포해야 합니다. CrossPromoKit은 그 화면을 패키지
하나로 대체하고, 앱 목록을 원격 JSON으로 빼서 **앱 재배포 없이 카탈로그만 갱신하면 되게**
만드는 것이 목적입니다.

설계에서 양보하지 않는 것:

- **오프라인이어도 조용히 동작한다.** 네트워크 실패는 사용자에게 보고할 사건이 아닙니다.
  만료된 캐시라도 빈 화면보다 낫다고 봅니다.
- **통합 비용이 낮아야 한다.** 몇 줄로 붙고, 서드파티 의존성을 끌고 오지 않습니다.
  프로모션 기능 하나 때문에 앱의 의존성 그래프가 커지면 안 됩니다.
- **처음부터 다국어·접근성.** 나중에 붙이면 안 붙습니다.

## 누구를 위한 것인가

앱을 여러 개 운영하는 인디 개발자와 소규모 팀. 광고 SDK를 붙일 만큼의 규모는 아니고,
자기 앱끼리 유입을 돌리고 싶은 쪽입니다. 광고 네트워크·수익 배분·트래킹은 목표가 아닙니다.

## 릴리스 상태

**미출시.** git 태그와 GitHub 릴리스가 아직 없습니다. SPM에서는 버전 범위 대신
브랜치나 커밋을 지정해 참조해야 합니다.

첫 태그를 자르는 것은 소유자 결정이라 지금까지 어떤 PR도 대신 하지 않았습니다(#34, PR #40).
태그를 자를 때 이 절을 버전 히스토리로 교체합니다.

## 다음에 할 것

이슈로 올라간 일은 이슈가 정본입니다. 여기에는 이슈가 없는 것만 적습니다.

- **첫 태그 자르기.** 남은 일은 버전 번호를 정하고, 양쪽 README의 SPM 설치 안내를
  브랜치 지정에서 버전 범위로 바꾸는 것입니다(#34/PR #40에서 태그 없음을 전제로 쓴 안내).

## 하지 않기로 한 것

"안 하기로 한 결정"은 이슈로 남지 않습니다. 닫힌 이슈만 봐서는 "안 함"인지 "나중에"인지
구분되지 않으므로 여기에 근거와 함께 남깁니다.

- **`.refreshable` 지원 / 서비스 주입 이니셜라이저** — 지금은 `forceRefresh: Binding<Bool>`
  토글입니다. 둘 다 공개 API 표면을 늘려야 하는데, 늘리지 않고도 필요한 동작이 나옵니다.
  보류(#6/PR #16, PR #46). 필요해지면 순수 추가로 언제든 얹을 수 있습니다.
- **`PromoConfig`에 타임아웃·재시도 정책 노출** — `init(urlSession:)`이 이미 공개된 조정
  수단이라 같은 설정을 두 군데서 표현하게 됩니다. 재시도는 오프라인 폴백 설계와 얽혀
  별도 논의가 필요합니다(#33/PR #43).
- **캐시 키 마이그레이션** — 캐시 키를 카탈로그 URL별로 쪼갤 때, 기존 전역 키의 데이터가
  어느 URL 것인지 알 수 없어 옮기지 않았습니다. 업데이트 후 첫 실행에서 캐시 미스 한 번을
  감수합니다(#7/PR #21).
- **CI에서 SwiftLint 바이너리 캐싱** — 다운로드가 1초 미만이라 `actions/cache` 왕복
  오버헤드가 더 큽니다. 더 무거운 툴체인을 고정하게 되면 재검토합니다(PR #27).
- **`file_header` 린트 룰** — 강제할 헤더 규약이 없고 LICENSE가 저작권 표기를 대신합니다.
  규약을 정하면 `file_header:` 블록과 함께 다시 켭니다(PR #23).
- **spec-kit 기반 진행** — 도구와 산출물을 걷어냈습니다(#26). 작업 단위는 spec-kit 태스크가
  아니라 GitHub 이슈이고, `docs/specs/`는 코드의 `FR-###` 참조를 위한 히스토리로만 둡니다.
- **분석 수집 내장** — 이벤트는 `PromoEventDelegate`로 호출자에게 넘기기만 하고, 이
  패키지는 아무것도 저장·전송하지 않습니다. 분석 도구 선택은 호스트 앱의 몫입니다.
